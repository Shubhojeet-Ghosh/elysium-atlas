/** Fixed banner strip height — keeps monitor + lead banners aligned during rotation. */
export const conversationBannerHeightClass = "h-16 min-h-16";

export const conversationBannerShellClass = `flex ${conversationBannerHeightClass} items-center px-3 shrink-0`;